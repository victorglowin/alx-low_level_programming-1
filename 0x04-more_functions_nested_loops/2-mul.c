#include "main.h"
#include <stdio.h>

/**
 * mul - multiply the integers
 * @a: First integer
 * @b: Second integer
 * 
 * Return: Return product of the two ints
 */
int mul(int a, int b);
{
    return (a * b);
}
